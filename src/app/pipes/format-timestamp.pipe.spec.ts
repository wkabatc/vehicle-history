import { FormatTimestampPipe } from './format-timestamp.pipe';

describe('FormatTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
